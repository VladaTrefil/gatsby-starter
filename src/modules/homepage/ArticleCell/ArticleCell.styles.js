export const main = () => ({})

export const image = ({ theme }) => ({
    borderRadius: theme.radius.big,
    overflow: "hidden",
})

export const infobar = () => ({
    margin: "12px -10px",
    display: "flex",
})

export const tag = () => ({
    margin: "0 10px",
})
