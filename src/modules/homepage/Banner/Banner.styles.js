export const inner = ({ theme }) => ({
    padding: "64px",
    background: theme.colors.cyanLight,
})

export const title = ({ theme }) => ({
    maxWidth: "785px",
    margin: "0 auto 80px",
    textAlign: "center",
})
